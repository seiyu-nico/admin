import date from './date';

const state = {
}

const getters = {
}

const mutations = {
}

const actions = {
}

const common = {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
  modules: {
    date,
  }
}

export default common